<!DOCTYPE html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Admin Dashboard | Meyer Vitabiotics</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body class="admin-body"><header class="admin-nav"><img src="{{ asset('assets/meyer-vitabiotics.jpg') }}" alt="Meyer Vitabiotics"><nav><a href="{{ route('admin.dashboard') }}" class="active">Dashboard</a><a href="#submissions">Listing</a><form method="POST" action="{{ route('admin.logout') }}">@csrf<button type="submit">Log out</button></form></nav></header><main class="dashboard-shell"><div class="dashboard-title"><div><span class="eyebrow">Admin panel</span><h1>Doctor video submissions</h1><p>All submitted employee and doctor details are listed below.</p></div><div class="count-card"><strong>{{ $submissions->total() }}</strong><span>Total submissions</span></div></div><section class="table-card" id="submissions"><div class="table-wrap"><table><thead><tr><th>Doctor</th><th>Employee code</th><th>City</th><th>Status</th><th>Submitted</th><th>Video</th></tr></thead><tbody>@forelse($submissions as $submission)<tr><td><div class="doctor-cell">@if($submission->photo_url)<img src="{{ $submission->photo_url }}" alt="">@endif<div><strong>{{ $submission->prefix }} {{ $submission->name }}</strong><small>#{{ $submission->id }}</small></div></div></td><td>{{ $submission->employee_code }}</td><td>{{ $submission->city }}</td><td><span class="status status-{{ $submission->video_status }}">{{ ucfirst($submission->video_status) }}</span></td><td>{{ $submission->created_at->format('d M Y, h:i A') }}</td><td>@if($submission->video_status==='completed')<a class="table-download" href="{{ route('admin.download',$submission) }}">Download</a>@else<span class="muted">Not available</span>@endif</td></tr>@empty<tr><td colspan="6" class="empty-state">No submissions yet.</td></tr>@endforelse</tbody></table></div><div class="pagination">{{ $submissions->links() }}</div></section></main></body></html>
